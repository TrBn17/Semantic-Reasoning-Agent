# Frontend (Next.js)

Minimal scaffold for ontology Phase A (home, ontology graph/builds/review).

Run from repo root:

```powershell
cd apps/frontend
npm install
npm run dev
```

Set `NEXT_PUBLIC_API_BASE_URL` if the API is not at `http://localhost:8000/api/v1`.
