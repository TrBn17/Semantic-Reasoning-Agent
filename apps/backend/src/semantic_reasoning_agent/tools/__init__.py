"""First-class execution units. A Tool consumes a ToolEnvelope and returns
a ToolResult — testable in isolation, composable into workflows.

R3 introduces the Tool ABC and the ontology family. Other families
(document, retrieval, graph) follow in later phases as their backing
service code is split out.
"""
